# Environment Configuration for ZoomClone Server

# Server Port (default: 5000)
PORT=5000

# Node Environment
NODE_ENV=production

# Optional: CORS allowed origins (for security)
# Leave empty or set to localhost for development
ALLOWED_ORIGINS=*
